<section class="hero">
    <div class="container container--hero">
        <div class="hero__content">
            <h1 class="hero__title">{{ __('home.hero_title') }}</h1>
            <p class="hero__description">{{ __('home.hero_description') }}</p>
            <x-link href="/shop" color="secondary" size="sm">{{ __('home.shop_now') }}</x-link>
        </div>
    </div>
</section>